%h1 Table

%ul#players
  - @table.players.each do |player|
    %li= player['user_name']

= button_to 'Start Game', table_games_path(@table), remote: true

#game
  = render 'games/show', game: @table.active_game&.game